import { Box } from "@mui/material";
import React, { FC, memo } from "react";

/**
 * Компонент страницы таба импорта на странице отчетов
 * @returns 
 */
const ImportPage: FC = () => {
  return (
    <Box>
      Import page
    </Box>
  );
};

export default memo(ImportPage);
