import { ListRowProps } from "components/ListPageParts/List/ListBody/ListRow";
import React, { FC, memo } from "react";

const PlayerCodeRow: FC<ListRowProps> = ({}) => {
  return (
    <>
      
    </>
  );
};

export default memo(PlayerCodeRow);
