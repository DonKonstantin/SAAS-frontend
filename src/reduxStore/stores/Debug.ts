// Store для хранения состояния отладки
export interface Debug {
    isEnabled: boolean
}

// Интерфейс типов событий для данного хранилища
export interface DebugActionTypes {
    DEBUG_CHANGE_STATE: boolean
}